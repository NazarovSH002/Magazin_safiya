---
description: Создать резервную копию MongoDB перед обновлением кода
---

# 🛡️ Резервное копирование перед обновлением

Этот workflow создает резервную копию данных MongoDB перед тем, как вы обновите код на GitHub/Render.

## Когда использовать?

- Перед каждым `git push`
- Перед обновлением кода на Render
- Перед внесением критических изменений

---

## Шаги

### 1. Убедитесь, что сервер запущен

Если сервер не запущен, запустите его:

```powershell
node server.js
```

Или используйте:

```powershell
.\Запустить_Магазин.bat
```

### 2. Создайте резервную копию

В новом окне PowerShell выполните:

```powershell
curl http://localhost:3000/api/backup -o "backups/backup_$(Get-Date -Format 'yyyy-MM-dd_HH-mm-ss').json"
```

### 3. Проверьте, что бэкап создан

```powershell
ls backups/ | Sort-Object LastWriteTime -Descending | Select-Object -First 5
```

### 4. Теперь можно безопасно обновлять код

```powershell
git add .
git commit -m "Update: безопасное обновление с бэкапом"
git push
```

---

## 📊 Автоматический бэкап + пуш

Если хотите всё сразу (бэкап + git push):

```powershell
# Создаем папку для бэкапов
if (!(Test-Path "backups")) { New-Item -ItemType Directory -Path "backups" }

# Создаем бэкап
Write-Host "🔄 Создание резервной копии..." -ForegroundColor Cyan
$backupFile = "backups/backup_$(Get-Date -Format 'yyyy-MM-dd_HH-mm-ss').json"
curl http://localhost:3000/api/backup -o $backupFile

# Проверяем размер файла
$fileSize = (Get-Item $backupFile).Length
if ($fileSize -gt 100) {
    Write-Host "✅ Бэкап создан: $backupFile ($fileSize байт)" -ForegroundColor Green
    
    # Теперь делаем git push
    Write-Host "📤 Отправка в GitHub..." -ForegroundColor Cyan
    git add .
    git commit -m "Update: $(Get-Date -Format 'yyyy-MM-dd HH:mm')"
    git push
    
    Write-Host "✅ Готово! Код обновлен, данные в безопасности!" -ForegroundColor Green
} else {
    Write-Host "❌ Ошибка: бэкап не создан. Проверьте, запущен ли сервер." -ForegroundColor Red
}
```

---

## 🆘 Восстановление из бэкапа

Если что-то пошло не так и нужно восстановить данные:

### Вариант 1: Через веб-интерфейс

1. Откройте приложение
2. Войдите как администратор
3. Перейдите на вкладку "Пользователи"
4. Нажмите "Импорт данных"
5. Выберите файл бэкапа из папки `backups/`

### Вариант 2: Через API

```powershell
$backupContent = Get-Content "backups/backup_2026-02-14_00-40-00.json" -Raw
Invoke-RestMethod -Uri "http://localhost:3000/api/restore" -Method POST -Body $backupContent -ContentType "application/json"
```

---

## 💡 Советы

1. **Делайте бэкапы регулярно** - перед каждым важным изменением
2. **Храните бэкапы локально** - папка `backups/` не загружается в Git
3. **Проверяйте размер бэкапа** - если файл слишком маленький, возможно, сервер не запущен
4. **Старые бэкапы удаляются автоматически** - хранятся последние 10 копий

---

**Важно:** Этот workflow работает только для локального сервера. Для Render используйте веб-интерфейс MongoDB Atlas для бэкапов.
