---
description: Быстрое сохранение изменений и отправка в GitHub
---

# 🚀 Безопасное обновление кода

## ⚠️ ВАЖНО: Перед первым использованием

Убедитесь, что на Render настроена переменная окружения `MONGODB_URI`:

1. Откройте <https://render.com>
2. Выберите ваш проект
3. Перейдите в "Environment"
4. Добавьте переменную `MONGODB_URI` со значением из файла `.env`

**Подробная инструкция:** см. файл `RENDER_SETUP.md`

---

## Шаги обновления

// turbo-all

### 1. Сохранить изменения и отправить в GitHub

```powershell
git add .
git commit -m "Update: $(Get-Date -Format 'yyyy-MM-dd HH:mm')"
git push
```

### 2. Проверить статус на Render

После выполнения команды:

- Render автоматически обновит код (1-2 минуты)
- Данные MongoDB останутся нетронутыми
- Проверьте логи Render: должно быть "✅ Успешно подключено к MongoDB"

---

## 📊 Статус

✅ **Код обновлен в GitHub**  
⏳ **Render обновляется автоматически** (подождите 1-2 минуты)  
🔒 **База данных MongoDB Atlas в безопасности**

---

## 🆘 Если что-то пошло не так

1. **Проверьте переменные окружения на Render** - должна быть `MONGODB_URI`
2. **Проверьте логи Render** - ищите ошибки подключения к MongoDB
3. **Создайте бэкап** - используйте `/backup` перед критическими изменениями
