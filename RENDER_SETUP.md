# 🚀 Настройка Render для сохранения данных MongoDB

## ⚠️ ПРОБЛЕМА

При каждом обновлении кода на Render база данных сбрасывается, потому что Render не знает о вашей MongoDB Atlas.

## ✅ РЕШЕНИЕ

Добавить переменную окружения `MONGODB_URI` в настройки Render.

---

## 📋 Пошаговая инструкция

### Шаг 1: Скопируйте строку подключения MongoDB

Ваша строка подключения (из файла `.env`):

```text
mongodb+srv://shopuser:Safiya2026@cluster0.jut4x8p.mongodb.net/shop_manager?retryWrites=true&w=majority
```

**ВАЖНО:** Скопируйте эту строку полностью!

---

### Шаг 2: Откройте настройки вашего проекта на Render

1. Перейдите на сайт: <https://render.com>
2. Войдите в свой аккаунт
3. Найдите ваш проект **shop-manager** (или как вы его назвали)
4. Нажмите на название проекта

---

### Шаг 3: Добавьте переменную окружения

1. В меню слева выберите **"Environment"** (Окружение)
2. Нажмите кнопку **"Add Environment Variable"** (Добавить переменную окружения)
3. Заполните поля:
   - **Key (Ключ):** `MONGODB_URI`
   - **Value (Значение):** `mongodb+srv://shopuser:Safiya2026@cluster0.jut4x8p.mongodb.net/shop_manager?retryWrites=true&w=majority`
4. Нажмите **"Save Changes"** (Сохранить изменения)

---

### Шаг 4: Перезапустите сервис

1. Render автоматически перезапустит ваш сервис
2. Подождите 1-2 минуты
3. Проверьте логи (вкладка "Logs")
4. Вы должны увидеть: `✅ Успешно подключено к MongoDB`

---

## 🎯 Что это даст?

✅ **Данные сохраняются навсегда** - MongoDB Atlas хранит данные в облаке  
✅ **Обновления кода безопасны** - база данных не затрагивается  
✅ **Синхронизация между устройствами** - одна база для всех  
✅ **Автоматические бэкапы** - MongoDB Atlas делает резервные копии  

---

## 🔍 Проверка подключения

После настройки откройте логи Render и найдите строку:

```text
📡 Попытка подключения к MongoDB...
✅ Успешно подключено к MongoDB
👤 Дефолтный админ создан в MongoDB
Сервер запущен на порту: 10000
```

Если видите эти сообщения - **всё работает правильно!**

---

## ⚠️ Важные замечания

### 1. Безопасность пароля

Ваш пароль MongoDB (`Safiya2026`) виден в строке подключения. Рекомендую:

- Не делиться этим файлом публично
- Не коммитить `.env` в GitHub (уже добавлен в `.gitignore`)

### 2. Файл `.env` НЕ загружается на Render

Render **не видит** ваш локальный файл `.env`, поэтому нужно добавить переменные вручную через веб-интерфейс.

### 3. Локальная разработка

На вашем компьютере всё работает через файл `.env`, поэтому ничего менять не нужно.

---

## 🆘 Если что-то пошло не так

### Проблема: "Ошибка подключения к MongoDB"

**Решение:** Проверьте:

1. Правильно ли скопирована строка подключения (без пробелов)
2. Доступен ли MongoDB Atlas (зайдите на <https://cloud.mongodb.com>)
3. Не истек ли срок действия пароля

### Проблема: "База данных пустая после обновления"

**Решение:**

1. Проверьте, что переменная `MONGODB_URI` добавлена в Render
2. Перезапустите сервис на Render
3. Используйте `/api/backup` для создания резервной копии

---

## 📞 Дополнительная помощь

Если нужна помощь, проверьте:

- Логи Render (вкладка "Logs")
- Статус MongoDB Atlas (<https://cloud.mongodb.com>)
- Переменные окружения (вкладка "Environment")

---

**Дата создания:** 14.02.2026  
**Версия:** 1.0
